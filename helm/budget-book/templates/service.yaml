apiVersion: v1
kind: Service
metadata:
  name: {{ include "budget-book.fullname" . }}
  labels:
    {{- include "budget-book.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "budget-book.selectorLabels" . | nindent 4 }}
